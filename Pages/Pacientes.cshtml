@page
@model ClinicScheduler_Web.Pages.PacientesModel
@{
    ViewData["Title"] = "Pacientes";
}

<div class="container mt-4">
    <h4>Pacientes</h4>

    <form method="post">
        @Html.AntiForgeryToken()
        <div class="row g-3 mb-4">
            <div class="col-md-4">
                <label class="form-label">Nome</label>
                <input type="text" name="nome" class="form-control" required />
            </div>
            <div class="col-md-3">
                <label class="form-label">Data de Nascimento</label>
                <input type="date" name="dataNascimento" class="form-control" required />
            </div>
            <div class="col-md-3">
                <label class="form-label">Contato</label>
                <input type="text" name="contato" class="form-control" />
            </div>
            <div class="col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-primary w-100">Adicionar</button>
            </div>
        </div>
        <div class="mb-3">
            <label class="form-label">Observação</label>
            <input type="text" name="observacao" class="form-control" />
        </div>
    </form>

    @if (!string.IsNullOrEmpty(Model.Mensagem))
    {
        <div class="alert alert-success">@Model.Mensagem</div>
    }

    <hr />

    <div class="mb-3">
        <input type="text" id="busca" class="form-control" placeholder="Buscar por nome ou ID..." oninput="filtrar()" />
    </div>

    <table class="table table-bordered table-hover">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Data de Nascimento</th>
                <th>Contato</th>
                <th>Observação</th>
                <th>Ação</th>
            </tr>
        </thead>
        <tbody id="tabela">
            @foreach (var p in Model.Pacientes)
            {
                <tr>
                    <td>@p.Id</td>
                    <td>@p.Nome</td>
                    <td>@p.DataNascimento.ToString("dd/MM/yyyy")</td>
                    <td>@p.Contato</td>
                    <td>@p.Observacao</td>
                    <td>
                        <form method="post" action="/Pacientes?handler=Excluir" onsubmit="return confirm('Excluir este paciente?')">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="pacienteId" value="@p.Id" />
                            <button type="submit" class="btn btn-danger btn-sm">Excluir</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<script>
    function filtrar() {
        const busca = document.getElementById('busca').value.toLowerCase();
        const linhas = document.querySelectorAll('#tabela tr');
        linhas.forEach(linha => {
            const texto = linha.innerText.toLowerCase();
            linha.style.display = texto.includes(busca) ? '' : 'none';
        });
    }
</script>